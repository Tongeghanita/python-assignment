# Create tuple
t = (1, 2, 3)

# Access
print("First element:", t[0])

# Nested tuple
nested = (1, (2, 3), 4)
print("Nested tuple:", nested)

# Repetition
repeat = t * 2
print("Repeated tuple:", repeat)